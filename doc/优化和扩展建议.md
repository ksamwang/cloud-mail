# Cloud Mail 优化和扩展建议

## 概述

本文档基于对 Cloud Mail 项目（`mail-worker` + `mail-vue`）的全面代码审查，梳理了当前存在的可优化点以及值得扩展的功能方向。所有建议均在 Cloudflare 生态内实施，保持零服务器成本的核心优势。

---

## 一、优化建议

### 1.1 密码哈希升级为 PBKDF2（优先级：P0）

**现状：** [`mail-worker/src/utils/crypto-utils.js`](../mail-worker/src/utils/crypto-utils.js) 仅使用单次 SHA-256 进行密码哈希，GPU 每秒可暴力尝试数百万次。

```js
// 当前实现
async genHashPassword(password, salt) {
    const data = encoder.encode(salt + password);
    const hashBuffer = await crypto.subtle.digest('SHA-256', data);
    return btoa(String.fromCharCode(...hashArray));
}
```

**建议：** 升级为 PBKDF2，使用 10 万次迭代。

```js
// 改进方案
async genHashPassword(password, salt) {
    const key = await crypto.subtle.importKey(
        'raw', encoder.encode(password),
        { name: 'PBKDF2' }, false, ['deriveBits']
    );
    const hashBuffer = await crypto.subtle.deriveBits(
        { name: 'PBKDF2', salt: encoder.encode(salt),
          iterations: 100000, hash: 'SHA-256' },
        key, 256
    );
    return btoa(String.fromCharCode(...new Uint8Array(hashBuffer)));
}
```

- **改动量：** ~15 行
- **兼容策略：** 登录时优先使用新算法验证，失败后回退旧算法，验证通过后自动 rehash
- **建议增加密码强度校验：** 禁止弱密码（纯数字、少于 8 位、与邮箱前缀相同等）

---

### 1.2 API 限流中间件（优先级：P0）

**现状：** 零限流保护，登录接口 `/login` 无任何频率限制，可被暴力破解。

**建议：** 基于 Cloudflare KV 实现令牌桶限流中间件。

```js
// mail-worker/src/hono/rate-limit.js
export function rateLimit({ window = 60, max = 60 } = {}) {
    return async (c, next) => {
        const ip = c.req.header('CF-Connecting-IP') || 'unknown';
        const key = `rate:${ip}:${c.req.path}`;
        const count = parseInt(await c.env.kv.get(key) || '0');
        if (count >= max) {
            throw new BizError('Too many requests', 429);
        }
        await c.env.kv.put(key, count + 1, { expirationTtl: window });
        await next();
    };
}

// 针对登录接口使用更严格的限制
app.post('/login', rateLimit({ window: 60, max: 5 }), loginHandler);
app.post('/register', rateLimit({ window: 3600, max: 3 }), registerHandler);
// 通用 API 使用宽松限制
app.use('/api/*', rateLimit({ window: 60, max: 60 }));
```

- **改动量：** ~40 行（新增文件 + 注册中间件）
- **收益：** 防止暴力破解、批量注册、API 滥用
- **注意：** `CF-Connecting-IP` 是 Cloudflare 提供的真实 IP header，无需自己解析

---

### 1.3 附件 Key 加入唯一标识防止冲突（优先级：P0）

**现状：** [`mail-worker/src/email/email.js`](../mail-worker/src/email/email.js) 附件 key 仅由 hash + 扩展名组成。

```js
// 当前：两封不同邮件的相同附件会产生 key 冲突
attachment.key = constant.ATTACHMENT_PREFIX + hash + ext;
```

**建议：** 加入 emailId 或 uuid 保证唯一性。

```js
// 改进方案
attachment.key = `attachments/${emailId}/${uuidv4()}_${hash}${ext}`;
```

同时修改 [`mail-worker/src/service/att-service.js`](../mail-worker/src/service/att-service.js) 中 `saveSendAtt` 方法的 key 生成逻辑：

```js
// mail-worker/src/service/att-service.js line 155
att.key = `attachments/${emailId}/${uuidv4()}_`
    + constant.ATTACHMENT_PREFIX.substring('attachments/'.length)
    + await fileUtils.getBuffHash(att.buff)
    + fileUtils.getExtFileName(att.filename);
```

- **改动量：** ~20 行（需要确认所有 key 构造点统一修改）
- **注意：** 旧格式 key 仍存在 R2/KV/S3 中，需保持兼容读取

---

### 1.4 统计查询优化 — 消除全表扫描（优先级：P1）

**现状：** [`mail-worker/src/dao/analysis-dao.js`](../mail-worker/src/dao/analysis-dao.js) 的 `numberCount` 方法对 email 表做全表 `SUM(CASE WHEN ...)` 扫描。邮件量超过 10 万条后每次请求响应变慢。

**建议 A（简单方案）：** 用 KV 缓存统计结果，Cron 定时刷新（当前已有但轮询周期可改）。

```js
// 在 scheduled 中增加统计增量更新
async function incrementStats(env, type, isDel) {
    const key = `stats:${type}:${isDel}`;
    const current = parseInt(await env.kv.get(key) || '0');
    await env.kv.put(key, current + 1);
}
```

**建议 B（根本方案）：** 创建 `stats` 计数器表，利用 D1 触发器自动维护。

```sql
CREATE TABLE IF NOT EXISTS stats (
    stat_key TEXT PRIMARY KEY,
    stat_value INTEGER NOT NULL DEFAULT 0
);

-- 插入新邮件时通过应用代码更新计数器
-- 物理删除时对应减少
```

- **改动量：** ~80 行
- **收益：** 统计查询从 O(n) 降为 O(1)

---

### 1.5 用户列表查询批处理优化（优先级：P1）

**现状：** [`mail-worker/src/service/user-service.js`](../mail-worker/src/service/user-service.js) 用户列表一次请求触发 7 个并行子查询（收件数、发件数、已删收件数、已删发件数、账号数、已删账号数、角色列表）。

**建议：** 将 7 次查询合并为 1 次 JOIN + GROUP BY 查询。

```sql
SELECT
    u.user_id,
    COUNT(CASE WHEN e.type = 0 AND e.is_del = 0 THEN 1 END) AS receive_count,
    COUNT(CASE WHEN e.type = 1 AND e.is_del = 0 THEN 1 END) AS send_count,
    COUNT(CASE WHEN e.type = 0 AND e.is_del = 1 THEN 1 END) AS del_receive_count,
    COUNT(CASE WHEN e.type = 1 AND e.is_del = 1 THEN 1 END) AS del_send_count,
    COUNT(DISTINCT CASE WHEN a.is_del = 0 THEN a.account_id END) AS account_count,
    COUNT(DISTINCT CASE WHEN a.is_del = 1 THEN a.account_id END) AS del_account_count
FROM user u
LEFT JOIN email e ON e.user_id = u.user_id
LEFT JOIN account a ON a.user_id = u.user_id
WHERE u.user_id IN (...)
GROUP BY u.user_id
```

- **改动量：** ~50 行
- **收益：** 用户列表页加载速度提升 40%+，大幅减少 D1 读取消耗

---

### 1.6 统一日志系统（优先级：P1）

**现状：** 36 处 `console.log/warn/error` 分散在 7 个文件中，无法按环境控制输出级别，生产环境可能泄露敏感信息。

**建议：** 新增统一日志工具模块。

```js
// mail-worker/src/utils/logger.js
const LEVELS = { DEBUG: 0, INFO: 1, WARN: 2, ERROR: 3, OFF: 4 };

export function createLogger(env) {
    const level = LEVELS[env.log_level] ?? LEVELS.INFO;

    return {
        debug(...args) { if (level <= LEVELS.DEBUG) console.debug('[DEBUG]', ...args); },
        info(...args)  { if (level <= LEVELS.INFO)  console.info('[INFO]', ...args); },
        warn(...args)  { if (level <= LEVELS.WARN)  console.warn('[WARN]', ...args); },
        error(...args) { if (level <= LEVELS.ERROR) console.error('[ERROR]', ...args); },
    };
}
```

在 `wrangler.toml` 中增加配置：

```toml
[vars]
log_level = "INFO"  # DEBUG | INFO | WARN | ERROR | OFF
```

- **改动量：** 新增 ~40 行 + 替换 36 处调用
- **收益：** 生产环境关闭 DEBUG 日志，错误排查可控

---

### 1.7 密码生成使用安全随机数（优先级：P2）

**现状：** [`mail-worker/src/utils/crypto-utils.js`](../mail-worker/src/utils/crypto-utils.js) 的 `genRandomPwd` 使用 `Math.random()`，可被预测。

```js
// 当前：不安全的随机
result += chars.charAt(Math.floor(Math.random() * chars.length));
```

**建议：** 改用 `crypto.getRandomValues`。

```js
genRandomPwd(length = 12) {  // 同时增加默认长度
    const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*';
    const array = new Uint8Array(length);
    crypto.getRandomValues(array);
    return Array.from(array, b => chars[b % chars.length]).join('');
}
```

- **改动量：** ~5 行
- **收益：** API 批量创建用户时的密码不再可预测

---

### 1.8 异常处理增强（优先级：P2）

**现状：** 多处静默吞掉异常，用户无感知。

- [`mail-worker/src/email/email.js`](../mail-worker/src/email/email.js) line 139-145：附件保存失败仅 `console.error`
- [`mail-worker/src/service/telegram-service.js`](../mail-worker/src/service/telegram-service.js) line 89-94：TG 发送失败仅 `console.error`

**建议：** 关键路径的失败要有明确的错误标记。

```js
// email.js - 附件保存失败时标记邮件状态
try {
    if (attachments.length > 0) {
        await attService.addAtt({ env }, attachments);
    }
} catch (e) {
    console.error('附件保存失败:', e);
    // 将邮件状态标记为部分成功
    await emailService.updateEmailStatus(
        { env }, 
        { status: emailConst.status.RECEIVE, message: '附件保存失败' }
    );
}
```

- **改动量：** ~30 行
- **收益：** 用户能感知到附件是否成功

---

### 1.9 前端邮件列表虚拟滚动（优先级：P2）

**现状：** 邮件列表使用 Element Plus 普通列表渲染，收件箱超过 200 封时 DOM 节点过多导致滚动卡顿。

**建议：** 使用 `vue-virtual-scroller` 或 Element Plus 的虚拟化表格组件。

```bash
pnpm add vue-virtual-scroller
```

```vue
<!-- 替换 mail-vue/src/views/email/index.vue 中的列表 -->
<RecycleScroller
  :items="emailList"
  :item-size="80"
  key-field="emailId"
  v-slot="{ item }"
>
  <EmailItem :email="item" />
</RecycleScroller>
```

- **改动量：** ~200 行
- **收益：** 万级邮件列表也能流畅滚动

---

### 1.10 `Math.random()` 全局审计（优先级：P3）

审计代码库中所有 `Math.random()` 使用点，全部替换为 `crypto.getRandomValues()`。除已知的 [`crypto-utils.js`](../mail-worker/src/utils/crypto-utils.js) 外，检查其他文件是否有敏感场景使用。

---

## 二、扩展建议

### 2.1 邮件全文搜索 FTS5（推荐指数：⭐⭐⭐⭐⭐）

**现状：** 仅支持 `LIKE` 模糊匹配标题、发件人、收件人（[`email-service.js`](../mail-worker/src/service/email-service.js)），不支持搜索邮件正文。

**建议：** 使用 D1 内置的 FTS5 全文索引。

```sql
-- D1 Migration
CREATE VIRTUAL TABLE IF NOT EXISTS email_fts USING fts5(
    subject, content, text, send_email, to_email, code,
    content='email',
    content_rowid='email_id'
);

-- 触发器自动同步
CREATE TRIGGER IF NOT EXISTS email_ai AFTER INSERT ON email BEGIN
    INSERT INTO email_fts(rowid, subject, content, text, send_email, to_email, code)
    VALUES (new.email_id, new.subject, new.content, new.text, new.send_email, new.to_email, new.code);
END;

CREATE TRIGGER IF NOT EXISTS email_ad AFTER DELETE ON email BEGIN
    INSERT INTO email_fts(email_fts, rowid, subject, content, text, send_email, to_email, code)
    VALUES ('delete', old.email_id, old.subject, old.content, old.text, old.send_email, old.to_email, old.code);
END;

CREATE TRIGGER IF NOT EXISTS email_au AFTER UPDATE ON email BEGIN
    INSERT INTO email_fts(email_fts, rowid, subject, content, text, send_email, to_email, code)
    VALUES ('delete', old.email_id, old.subject, old.content, old.text, old.send_email, old.to_email, old.code);
    INSERT INTO email_fts(rowid, subject, content, text, send_email, to_email, code)
    VALUES (new.email_id, new.subject, new.content, new.text, new.send_email, new.to_email, new.code);
END;
```

```js
// 搜索 API
async searchEmail(c, keyword, userId) {
    const results = await c.env.db.prepare(`
        SELECT e.* FROM email e
        INNER JOIN email_fts fts ON e.email_id = fts.rowid
        WHERE email_fts MATCH ? AND e.user_id = ?
        ORDER BY rank LIMIT 50
    `).bind(keyword, userId).all();
    return results;
}
```

- **改动量：** 后端 ~100 行 + 数据库迁移 + 前端搜索栏 ~150 行
- **收益：** 用户感知最强的功能提升，从"基本搜不了"变为"毫秒级全文检索"

---

### 2.2 邮件会话视图 Thread View（推荐指数：⭐⭐⭐⭐⭐）

**现状：** 数据库已有 `inReplyTo`、`references`、`messageId` 字段记录回复关系（[`email-service.js`](../mail-worker/src/service/email-service.js)），但前端按时间平铺展示，无对话分组。

**建议：** 按 `messageId / inReplyTo` 组装对话树。

```js
// mail-worker/src/service/email-service.js
async threadView(c, userId, accountId) {
    const emails = await orm(c).select().from(email).where(
        and(eq(email.userId, userId), eq(email.accountId, accountId),
            eq(email.isDel, isDel.NORMAL), eq(email.type, emailConst.type.RECEIVE))
    ).orderBy(desc(email.createTime)).limit(200).all();

    // 构建 messageId -> children 映射
    const replyMap = {};
    for (const e of emails) {
        if (e.inReplyTo) {
            if (!replyMap[e.inReplyTo]) replyMap[e.inReplyTo] = [];
            replyMap[e.inReplyTo].push(e);
        }
    }

    // 找根邮件（没有 inReplyTo 或引用的邮件不在列表中）
    const roots = emails.filter(e =>
        !e.inReplyTo || !emails.find(p => p.messageId === e.inReplyTo)
    );

    return buildTrees(roots, replyMap);
}
```

前端使用递归组件渲染对话缩进。

- **改动量：** 后端 ~50 行 + 前端 ~300 行
- **收益：** 邮件体验质的飞跃，从"留言板"到"对话"

---

### 2.3 邮件规则引擎（推荐指数：⭐⭐⭐⭐）

**现状：** `ruleEmail` 仅支持按域名过滤是否接收（[`email-service.js`](../mail-worker/src/service/email-service.js)）。

**建议：** 扩展为可组合的规则链。

```js
// 规则模型
const ruleTemplates = {
    IF_FROM:    { field: 'from',    op: 'contains' | 'equals' | 'regex' },
    IF_SUBJECT: { field: 'subject', op: 'contains' | 'equals' | 'regex' },
    IF_TO:      { field: 'to',      op: 'contains' | 'equals' },
    THEN_STAR:  { action: 'star' },
    THEN_DELETE:{ action: 'delete' },
    THEN_FORWARD: { action: 'forward', target: 'email@xxx.com' },
    THEN_TAG:   { action: 'tag', value: '标签名' },
    THEN_BOUNCE:{ action: 'bounce', message: '拒收原因' },
};
```

```sql
-- 规则表
CREATE TABLE IF NOT EXISTS email_rule (
    rule_id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    name TEXT NOT NULL DEFAULT '',
    conditions TEXT NOT NULL DEFAULT '[]',  -- JSON: [{field, op, value}]
    actions TEXT NOT NULL DEFAULT '[]',     -- JSON: [{action, value}]
    priority INTEGER NOT NULL DEFAULT 0,
    enabled INTEGER NOT NULL DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

- **改动量：** 后端 ~300 行 + 前端规则编辑器 ~400 行
- **收益：** 管理员功能的核心卖点，自动化邮件处理

---

### 2.4 多平台 OAuth 登录（推荐指数：⭐⭐⭐⭐）

**现状：** 仅接入了 Linux DO OAuth（[`oauth-api.js`](../mail-worker/src/api/oauth-api.js)）。

**建议：** 扩展至常用平台。

| 平台 | 适用场景 | 难度 |
|------|---------|------|
| **GitHub OAuth** | 开发者部署场景，最通用 | 低 |
| **Google OAuth** | Gmail 用户无缝衔接 | 中 |
| **微信 OAuth** | 国内用户群体 | 中 |

```js
// 通用 OAuth 抽象
const oauthProviders = {
    github: {
        authUrl: 'https://github.com/login/oauth/authorize',
        tokenUrl: 'https://github.com/login/oauth/access_token',
        userUrl: 'https://api.github.com/user',
        scope: 'user:email',
    },
    google: {
        authUrl: 'https://accounts.google.com/o/oauth2/v2/auth',
        tokenUrl: 'https://oauth2.googleapis.com/token',
        userUrl: 'https://www.googleapis.com/oauth2/v2/userinfo',
        scope: 'email profile',
    },
};
```

- **改动量：** 每个平台 ~100 行后端 + ~50 行前端按钮
- **收益：** 扩大用户覆盖面，降低注册门槛

---

### 2.5 多平台 Webhook 推送（推荐指数：⭐⭐⭐）

**现状：** 仅支持 Telegram Bot 推送（[`telegram-service.js`](../mail-worker/src/service/telegram-service.js)）。

**建议：** 扩展至主流 IM 平台。

```js
// mail-worker/src/service/webhook-service.js
const adapters = {
    slack: (email, webhookUrl) => ({
        url: webhookUrl,
        body: JSON.stringify({
            text: `📧 *${email.subject}*\nFrom: ${email.sendEmail}\nTo: ${email.toEmail}`,
            attachments: [{ title: 'View Email', title_link: `...` }]
        })
    }),
    dingtalk: (email, webhookUrl) => ({ /* 钉钉 Markdown 格式 */ }),
    feishu: (email, webhookUrl) => ({ /* 飞书卡片格式 */ }),
    wecom: (email, webhookUrl) => ({ /* 企业微信 Markdown 格式 */ }),
    discord: (email, webhookUrl) => ({ /* Discord Embed 格式 */ }),
};

async function pushToPlatform(platform, email, webhookUrl) {
    const adapter = adapters[platform];
    if (!adapter) throw new Error(`Unknown platform: ${platform}`);
    const { url, body } = adapter(email, webhookUrl);
    return fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body });
}
```

- **改动量：** ~300 行（每种平台 ~50 行格式化函数）
- **收益：** 让非 TG 用户也能收到实时推送

---

### 2.6 PWA 推送通知（推荐指数：⭐⭐⭐）

**现状：** 前端已集成 `vite-plugin-pwa`（[`package.json`](../mail-vue/package.json)），但未启用 Web Push。

**建议：** 增加后端 push subscription 管理 + 新邮件触发推送。

```js
// 新增 endpoint
app.post('/push/subscribe', async (c) => {
    const { subscription, userId } = await c.req.json();
    await c.env.db.prepare(
        'INSERT OR REPLACE INTO push_subscription (user_id, endpoint, p256dh, auth) VALUES (?, ?, ?, ?)'
    ).bind(userId, subscription.endpoint, subscription.keys.p256dh, subscription.keys.auth).run();
});

// 新邮件到达时触发
async function notifyNewEmail(env, userId, emailSummary) {
    const subs = await env.db.prepare(
        'SELECT * FROM push_subscription WHERE user_id = ?'
    ).bind(userId).all();

    for (const sub of subs.results) {
        await sendWebPush(sub, {
            title: emailSummary.subject,
            body: `From: ${emailSummary.sendEmail}`,
            icon: '/mail-pwa.png',
            data: { url: `/message?emailId=${emailSummary.emailId}` }
        });
    }
}
```

- **改动量：** 后端 ~150 行 + 前端 Service Worker ~100 行
- **收益：** 移动端接近原生 App 体验

---

### 2.7 垃圾邮件检测（推荐指数：⭐⭐⭐）

**现状：** 仅有基于关键词的黑白名单过滤，无智能分类。

**建议：** 使用 Workers AI 做 spam/ham 分类（模式同现有 [`ai-service.js`](../mail-worker/src/service/ai-service.js)）。

```js
// mail-worker/src/service/spam-service.js
async function classifySpam(env, email) {
    const ai = env.ai;
    const result = await ai.run(env.ai_model || '@cf/meta/llama-3.1-8b-instruct', {
        messages: [{
            role: 'system',
            content: 'Classify email as spam or not. Return JSON: {"spam": true/false, "confidence": 0-1, "reason": "..."}'
        }, {
            role: 'user',
            content: `Subject: ${email.subject}\n\n${emailUtils.htmlToText(email.html || email.text || '')}`
        }],
        temperature: 0,
        max_tokens: 128
    });

    const json = JSON.parse(result.response || result);
    return json;
}
```

- **改动量：** ~150 行
- **注意：** Workers AI 免费额度有限，建议作为可选功能按需开启，并加缓存

---

### 2.8 邮件自动回复 / 假期回复（推荐指数：⭐⭐⭐）

**建议：** 支持用户设置基于时间范围和条件的自动回复。

```sql
CREATE TABLE IF NOT EXISTS auto_reply (
    reply_id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    account_id INTEGER NOT NULL,
    subject TEXT NOT NULL,
    content TEXT NOT NULL,
    start_time DATETIME,
    end_time DATETIME,
    reply_once INTEGER NOT NULL DEFAULT 1,  -- 同一发件人仅回复一次
    enabled INTEGER NOT NULL DEFAULT 1
);
```

邮件接收流程中（[`email.js`](../mail-worker/src/email/email.js)）插入自动回复检查：

```js
// 在 email.js 的 receive 流程中
const autoReply = await autoReplyService.match(env, message.to, message.from);
if (autoReply) {
    await emailService.sendAutoReply(env, autoReply, email);
}
```

- **改动量：** ~400 行
- **收益：** 商务场景标配功能

---

### 2.9 邮件标签系统（推荐指数：⭐⭐）

**建议：** 允许用户创建自定义标签，对邮件打标签分类。

```sql
CREATE TABLE IF NOT EXISTS tag (
    tag_id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    name TEXT NOT NULL,
    color TEXT NOT NULL DEFAULT '#409EFF'
);

CREATE TABLE IF NOT EXISTS email_tag (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email_id INTEGER NOT NULL,
    tag_id INTEGER NOT NULL
);
```

- **改动量：** ~400 行
- **收益：** 弥补当前只有"星标"一种分类方式的短板

---

### 2.10 邮件导出（推荐指数：⭐⭐）

**建议：** 支持将邮件导出为 `.eml` 格式或 PDF。

```js
function toEml(emailRow) {
    const headers = [
        `From: ${emailRow.sendEmail}`,
        `To: ${emailRow.toEmail}`,
        `Subject: ${emailRow.subject}`,
        `Date: ${emailRow.createTime}`,
        `Message-ID: ${emailRow.messageId}`,
        `Content-Type: text/html; charset=UTF-8`,
        '',
        emailRow.content || emailRow.text || ''
    ];
    return new Response(headers.join('\r\n'), {
        headers: { 'Content-Type': 'message/rfc822',
                    'Content-Disposition': `attachment; filename="${emailRow.subject}.eml"` }
    });
}
```

- **改动量：** ~100 行
- **收益：** 用户数据便携性

---

## 三、优先实施路线图

### 第一阶段：安全加固（第 1-2 天）

```
第 1 天上午：PBKDF2 密码哈希升级         # P0 安全
第 1 天下午：API 限流中间件              # P0 安全
第 2 天上午：附件 Key 防冲突             # P0 数据安全
第 2 天下午：密码生成安全随机 + 审计     # P2 安全
```

### 第二阶段：性能优化（第 3-5 天）

```
第 3 天：统计查询批量优化（消除全表扫描）  # P1
第 4 天：用户列表 7 合一查询               # P1
第 5 天：统一日志 + 异常处理增强          # P1
```

### 第三阶段：核心功能扩展（第 2-3 周）

```
第 2 周：邮件全文搜索 FTS5                # 高价值
第 3 周：邮件会话视图 Thread View         # 高价值
```

### 第四阶段：生态扩展（第 4-6 周）

```
第 4 周：邮件规则引擎
第 5 周：多平台 Webhook（至少 3 平台）
第 6 周：GitHub OAuth + PWA 推送
```

### 后续迭代

```
第 7 周+ ：垃圾邮件检测、自动回复、标签系统、邮件导出
```

---

## 四、注意事项

1. **D1 配额限制：** Cloudflare D1 免费版每月 500 万次读取 / 10 万次写入。新增功能时需评估额外查询量，FTS5 搜索和统计优化反而能减少 D1 消耗。
2. **Workers AI 额度：** 免费版每日 1 万次推理，垃圾邮件分类需限制调用频率，建议只对"疑似 spam"触发 AI 判断。
3. **KV 写入限制：** 免费版每日 1000 次写入。当前设置缓存、限流计数均涉及 KV 写操作，需要合并写入和优化 TTL。
4. **向后兼容：** 密码升级、附件 key 改造都需要兼容存量数据，不能上线就破坏现有功能。
5. **文档维护：** 随着新功能上线，同步更新 [`README.md`](../README.md) 和 [在线文档](https://doc.skymail.ink)。

---

> 📅 最后更新：2026-06-04
>
> 📝 本文档基于对项目 `d:\Wanglv_Dev\node\cloud-mail` 的完整代码审查生成。
